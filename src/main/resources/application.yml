server:
  port: 8080

spring:
  datasource:
#    url: jdbc:mysql://quiz-reward-database.cbi8c0c4kdx2.ap-northeast-2.rds.amazonaws.com:3306/<DB-NAME>?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    url: jdbc:mysql://localhost:3306/quiz_system?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

logging:
  level:
    root: INFO
    org.springframework.jdbc.core: DEBUG # ?? ?? ?? ?? ??
